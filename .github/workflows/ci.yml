name: Workplace CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  backend-quality:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify lockfile exists
        run: test -f package-lock.json

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npm run prisma:generate --workspace backend-api

      - name: Check generated contracts are up-to-date
        run: npm run contracts:check

      - name: Backend lint
        run: npm run lint --workspace backend-api

      - name: Backend typecheck
        run: npm run typecheck --workspace backend-api

      - name: Backend unit and integration tests
        run: npm run test --workspace backend-api

  mobile-quality:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify lockfile exists
        run: test -f package-lock.json

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Mobile lint
        run: npm run lint --workspace mobile-app

      - name: Mobile typecheck
        run: npm run typecheck --workspace mobile-app

      - name: Mobile tests
        run: npm run test --workspace mobile-app

      - name: Enforce mobile API fetch policy
        run: npm run contracts:check-fetch

      - name: Verify mobile E2E flow specs
        run: |
          test -f mobile-app/e2e/maestro/feed-message-flow.yaml
          test -f mobile-app/e2e/maestro/invite-group-flow.yaml

  security-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify lockfile exists
        run: test -f package-lock.json

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Dependency audit gate
        run: node ./scripts/audit-gate.js
