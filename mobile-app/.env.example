# Feature flags used by the app at build/runtime
EXPO_PUBLIC_FLAG_AUTH_SESSION_REFRESH=true
EXPO_PUBLIC_FLAG_FLASHLIST_RENDERING=true

# Optional metadata values for release tracking
EXPO_PUBLIC_RELEASE_CHANNEL=internal
EXPO_PUBLIC_RELEASE_VERSION=0.1.0